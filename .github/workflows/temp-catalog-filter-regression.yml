name: TEMP catalog filter regression

on:
  push:
    branches:
      - fix/catalog-filter-taxonomy-20260919

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diff check
        run: git diff --check origin/main...HEAD
      - name: Catalog filter regression
        run: python scripts/test_catalog_filters.py

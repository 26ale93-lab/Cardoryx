name: TEMP verified Normal Holo values

on:
  push:
    branches:
      - fix/verified-normal-holo-values-20260918
  workflow_dispatch:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Diff check
        run: |
          git fetch origin main --depth=1
          git diff --check origin/main...HEAD
      - name: Focused runtime regression
        run: |
          python - <<'PY'
          import importlib.util
          from pathlib import Path
          p=Path('scripts/test_card_identity_cardmarket_audit.py')
          spec=importlib.util.spec_from_file_location('audit',p)
          mod=importlib.util.module_from_spec(spec)
          spec.loader.exec_module(mod)
          result=mod.runtime_cardmarket_regression()
          print(result.get('erikasGloom'))
          PY
